syntax = "proto3";

package tosui.api;

import "go-micro_playground/proto/demo.proto";
import "google/api/annotations.proto";

service Tosui {
    rpc Hello(Request) returns (Response) {
        option (google.api.http) = {
			post: "/api/hello"
			body: "*"
		};
    }
}